<local:ImageButtonUserControl 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Excellent.Controls"
             mc:Ignorable="d" 
             x:Name="UserControl"
             x:Class="Excellent.Controls.ImageButton"
             d:DesignHeight="60" d:DesignWidth="225" d:DataContext="{d:DesignInstance Type=local:ImageButton, IsDesignTimeCreatable=False}">
    <Button d:DesignHeight="50" MinWidth="210" BorderBrush="Transparent">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Image Source="{Binding Path=ButtonImage}"></Image>
            <TextBlock VerticalAlignment="Center" Text="{Binding Path=ButtonText}"></TextBlock>
        </StackPanel>
    </Button>
    
</local:ImageButtonUserControl>

